# Maintainer / developer docs (not needed for end users)
CLAUDE.md
DECISIONS.md
WIZARD_PROFILES.md
UI_PRODUCTION_PATTERNS.md
BROWSER_SMOKE.md
SANITY_REPORT.md
Architecture.md
MAINTAINER_GUIDE.md
skill.md
ТЗ.md

# Dev-only: tests and tooling
# Аудит #16 (PATCH 2.19.3): tests/unit/architecture/ — это executable-инварианты
# архитектуры (контракты production-кода), не dev tooling. Они tracked в репо
# чтобы следующая сессия / контрибьютор / CI имели доступ к invariants как к
# части кода. tests/integration/ и tests/unit/ остальное — dev-only по контракту.
tests/*
!tests/unit/
tests/unit/*
!tests/unit/architecture/
scripts/

# Provider price sources (maintainer-only; runtime uses js/data/providers-bundled.generated.js)
# IMPORTANT: leading slash anchors to repo root — without it, the pattern would
# also match js/data/, which holds the runtime providers bundle.
/data/

# Workshop / temp artifacts
_demo-screenshot.html
.playwright-mcp/
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.bak
*.old
*.tmp
*.swp

# Node
node_modules/
npm-debug.log*
